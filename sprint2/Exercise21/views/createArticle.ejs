<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD"
      crossorigin="anonymous"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css"
      integrity="sha512-MV7K8+y+gLIBoVD59lQIYicR65iaqukzvf/nwasF0nqhPay5w/9lJmVM2hMDcnK1OnMGCdVK+iQrJ7lzPJQd1w=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
    <link rel="stylesheet" href="css/styles.css" />
  </head>
  <body>
    <%- include("partials/navbar") %>
    <div class="container createNewArticleForm">
      <div class="text-bg-light p-3 mt-2 border">
        <h3>Crear nuevo artículo</h3>
      </div>
      <form
        id="createForm"
        name="createForm"
        class="shadow-lg p-4 border needs-validation"
        method="post"
        action="/articulos/crear"
        enctype="multipart/form-data"
      >
        <div class="mb-3">
          <label for="title" class="form-label">Título *</label>
          <input
            type="text"
            class="form-control"
            id="title"
            placeholder="Ingresar título..."
            name="title"
            required
          />
          <div class="invalid-feedback">El campo título es obligatorio.</div>
        </div>
        <div class="mb-3">
          <label for="content" class="form-label">Contenido *</label>
          <input
            type="text"
            class="form-control"
            id="content"
            placeholder="Ingresar contenido..."
            name="content"
            required
          />
          <div class="invalid-feedback">El campo contenido es obligatorio.</div>
        </div>
        <div class="mb-3">
          <label for="image" class="form-label">Portada del artículo</label>
          <input
            type="file"
            class="form-control"
            id="image"
            placeholder="Ingresar url de la imágen..."
            name="image"
          />
          <div class="invalid-feedback">El campo contenido es obligatorio.</div>
        </div>
        <label for="user" class="form-label">Autor</label>
        <select id="user" name="user" class="form-select" aria-label="Default select example">
          <option value="1" selected>Selecciona tu usuario</option>
          <% for(user of users) {%>
          <option value="<%= user.id  %>"><%= user.firstname %> <%= user.lastname %></option>
          <% } %>
        </select>
        <button id="createArticle" class="btn btn-dark mt-3">Guardar artículo</button>
      </form>
      <a class="pt-5 d-block text-decoration-none" href="http://localhost:3000">← Volver</a>
    </div>
    <%- include("partials/footer") %>
    <script src="/js/script.js"></script>
  </body>
</html>
