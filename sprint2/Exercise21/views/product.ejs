<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Bootstrap demo</title>
    <!--  CSS BOOTSTRAP -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD"
      crossorigin="anonymous"
    />
    <!-- CSS FONT AWSOME -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css"
      integrity="sha512-MV7K8+y+gLIBoVD59lQIYicR65iaqukzvf/nwasF0nqhPay5w/9lJmVM2hMDcnK1OnMGCdVK+iQrJ7lzPJQd1w=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
    <!-- LOCAL CSS -->
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <%- include("partials/navbar") %>
    <div class="container mt-5 w-75">
      <h1><%= article.title %></h1>
      <small class="text-success"><%= article.user.firstname%> <%= article.user.lastname%></small>
      <small class="text-muted d-block"> <%= new Date(article.createdAt).toLocaleString() %></small>
      <img
        class="img-fluid mt-2 img-product w-100"
        src="/img/articlesImg/<%= article.image  %>"
        alt=""
      />
      <p class="mt-3"><%= article.content %></p>
      <hr class="text-success my-4" />
      <hr class="border border-dark border-1 opacity-75 my-4" />
      <!-- COMMENTS -->
      <div>
        <h4 class="mt-5">Conversation</h4>
        <hr />
        <p>
          Bienvenido a Tech-Blog! Por favor mantenga las conversaciones cautelosas. Chequee el
          <small class="text-success">Community Guidelines</small> por mas informacion.
        </p>
        <section>
          <% for(comment of comments) {%>
          <div class="border p-3">
            <small class="text-success"
              ><%= comment.user.firstname %> <%= comment.user.lastname %>:</small
            >
            <p class="mt-2"><%= comment.content %></p>
            <small class="text-muted d-block mb-2">
              <%= new Date(comment.createdAt).toLocaleString() %></small
            >
          </div>
          <% } %>
          <form action="/comentarios/crear/<%= article.id  %>" method="POST">
            <select
              id="user"
              name="user"
              class="form-select my-2 border-success"
              aria-label="Default select example"
            >
              <option value="1" selected>Selecciona el usuario</option>
              <% for(user of users) {%>
              <option value="<%= user.id  %>"><%= user.firstname %> <%= user.lastname %></option>
              <% } %>
            </select>
            <div class="form-floating">
              <textarea
                class="form-control"
                placeholder="Leave a comment here"
                id="floatingTextarea2"
                style="height: 100px"
                name="content"
                required
              ></textarea>
              <label for="floatingTextarea2">Comments...</label>
            </div>
            <button type="submit" class="btn btn-success mb-5 mt-3">Publicar Comentario</button>
          </form>
        </section>
      </div>

      <a href="/"><i class="fa fa-long-arrow-left" aria-hidden="true"></i> Back</a>
    </div>
    <%- include("partials/footer") %>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
